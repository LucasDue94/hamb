import hamb.Atendimento

model {
    Atendimento atendimento
}

json g.render(atendimento, [expand: ['registroAtendimento', 'usuario','usuario.perfil'], excludes: ['usuario.senha','usuario.ultimoAcesso'], resolveTemplate: false]) {
}
